name: Labeler

on: [ pull_request_target ]

jobs:
  label:
    name: Label
    runs-on: ubuntu-latest

    steps:
    - uses: actions/labeler@v2
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
