/**
 * Animated textures.
 */
  export default {
    animated:{
      2266:{
        frames:[2266, 2267, 2268, 2269].map(frame => `${frame}`),
        speed:0.025,
      },
      2374:{
        frames:[2374, 2375, 2376, 2377, 2378, 2379, 2380, 2381].map(frame => `${frame}`),
        speed:0.075,
      },
      429:{
        frames:[429, 430].map(frame => `${frame}`),
        speed:0.05,
      },
      431:{
        frames:[431, 432].map(frame => `${frame}`),
        speed:0.0125,
      }
    }
  }
